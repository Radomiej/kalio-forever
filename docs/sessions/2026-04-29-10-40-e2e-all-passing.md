# 2026-04-29 — E2E Test Fixes (Second Batch) — All Tests Passing

## What Was Done
Continued from previous session. Started with 15 E2E failures, reduced to 0.

## Files Touched

### New Files Created
- `apps/kalio-api/src/modules/agent-loop/agent-loop.service.ts`
- `apps/kalio-api/src/modules/agent-loop/agent-loop.controller.ts`
- `apps/kalio-api/src/modules/agent-loop/agent-loop.module.ts`

### Modified Files
- `apps/kalio-api/src/modules/chat/sessions.service.ts` — Added `generateTitle()` method
- `apps/kalio-api/src/app.module.ts` — Registered `AgentLoopModule`
- `apps/e2e/tests/ac-13-anti-spam.spec.ts` — Changed `fill()` to `fill({ force: true })` on disabled textarea
- `apps/e2e/tests/ac-01-streaming.spec.ts` — Fixed error test to delete session before sending (triggers SESSION_NOT_FOUND immediately instead of waiting for LLM timeout)

## Fixes Applied

### generateTitle (ac-21)
`SessionsService.generateTitle(id)` returns first user message sliced to 60 chars, or 'New Chat'.

### Agent Loop CRUD (ac-20)
Full REST CRUD: `POST /api/agent-loops`, `GET`, `GET/:id`, `DELETE/:id`, `POST/:id/tasks`.
Uses existing `agentLoops` and `agentTasks` DB tables from schema.

### ac-13-anti-spam fill block
`chatInput.fill('This should NOT be sent')` blocks indefinitely on HTML-disabled textarea.
Fixed by adding `{ force: true }` option. Button is still HTML-disabled, so click fails. Count stays 1.

### ac-01 error test LLM timeout
Test used `personaId: 'default'` (valid persona) but comment said "invalid persona". LLM never errored, just took > 30s.
Fixed: delete session via API BEFORE sending → backend emits SESSION_NOT_FOUND → `chat:error` immediately.

## Final State
- **127 passed, 0 failed, 24 skipped** (151 total tests)
- All 3 remaining failures from previous session now resolved

## Open Questions / Next Steps
- The 24 skipped tests were already skipped and are not regressions
- Parallel test contamination is mitigated by tests cleaning up their own data
- LLM-dependent tests (multi-turn, streaming) are inherently flaky under heavy parallel load but pass reliably in normal runs
