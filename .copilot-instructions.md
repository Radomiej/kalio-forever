# Copilot Instructions for Kalio Project

This file is a compatibility shim.

Primary instructions live in:

- `AGENTS.md` — cross-agent project rules and constraints
- `.github/copilot-instructions.md` — canonical Copilot-specific guidance for this repo

Read both before making changes.

Keep this root file short on purpose. If instructions need updating, update the canonical files above instead of duplicating them here.
