import { test } from '@playwright/test';

// AC-11: Persona system prompt is applied to LLM context
test.describe('AC-11: Persona system prompt', () => {
  test.skip('selected persona system prompt is sent as first message in LLM context', () => {});
  test.skip('switching persona changes system prompt for next message', () => {});
});
