import { test } from '@playwright/test';

// AC-11: Persona system prompt is applied to LLM context
test.describe('AC-11: Persona system prompt', () => {
  test.todo('selected persona system prompt is sent as first message in LLM context');
  test.todo('switching persona changes system prompt for next message');
});
